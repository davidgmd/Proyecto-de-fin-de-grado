<Window x:Class="ElEscribaDelDJ.View.Calendar.Calendario"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElEscribaDelDJ.View.Calendar"
        xmlns:SystemGlobalization="clr-namespace:System.Globalization;assembly=mscorlib"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Calendario"
        Background="NavajoWhite"
        SizeToContent="WidthAndHeight">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/View/Calendar/Calendario.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Style="{DynamicResource StackPanelTituloVentana}">
        <TextBlock Text="Anotar Partida en el Calendario"
                   Style="{DynamicResource TextoEncabezadoVentana}"/>
            <TextBlock Text="Autentifiquese antes que nada en google calendar o pulse en el botón para recordar sus credenciales"/>
            <StackPanel Style="{DynamicResource StackPanelBotonera}">
                <Button Style="{DynamicResource BotonLoginOutlook}">
                    <StackPanel Style="{DynamicResource StackPanelBotonOutlook}">
                        <TextBlock Text="Outlook, Proximamente"
                                   Style="{DynamicResource TextoStackPanelOutlook}"/>
                        <!-- <Image Height="20"
                               Source="\Images\icons\outlook.png"/>-->
                    </StackPanel>
                </Button>
                <Button x:Name="botonGoogleCalendar" 
                        Style="{DynamicResource BotonLoginGoogleCalendar}"
                        Click="botonGoogleCalendar_Click">
                    <StackPanel Style="{DynamicResource StackPanelBotonGoogleCalendar}">
                        <TextBlock Text="Google Calendar"
                                   Style="{DynamicResource TextoStackPanelGoogleCalendar}"/>
                        <Image Style="{DynamicResource ImagenStackPanelGoogleCalendar}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>

        <StackPanel Style="{DynamicResource StackPanelCalendario}">
            <Viewbox Style="{DynamicResource ViewBoxCalendario}">
                <Calendar Style="{DynamicResource Calendario}" 
                          x:Name="Calendar" 
                          DisplayDateChanged="Calendar_DisplayDateChanged" 
                          Loaded="Calendar_Loaded">
                    <Calendar.CalendarDayButtonStyle>
                        <Style TargetType="CalendarDayButton" BasedOn="{StaticResource {x:Type CalendarDayButton}}">
                            <EventSetter Event="Loaded" Handler="calendarButton_Loaded" />
                        </Style>
                    </Calendar.CalendarDayButtonStyle>
                </Calendar>
            </Viewbox>
        </StackPanel>

        <StackPanel Style="{DynamicResource StackPanelDatosCalendario}"
                    x:Name="ListaEventosGoogleCalendar">
            
            <StackPanel x:Name="StackPanelActualizarListView" 
                        Style="{DynamicResource StackPanelActualizar}" 
                        MouseDown="ActualizarListView_MouseDown">
                <TextBlock Text="Actualizar" Style="{DynamicResource TextoActualizar}"/>
                <Button x:Name="BotonActualizarListView" 
                        Style="{DynamicResource BotonActualizar}" 
                        Click="BotonActualizarListView_Click">
                    <Image Style="{DynamicResource IconoBotonActualizar}"/>
                </Button>
            </StackPanel>

            <ComboBox Style="{DynamicResource ComboBoxDatosEvento}" 
                      x:Name="DatosEvento"
                      SelectionChanged="DatosEvento_SelectionChanged"
                      Binding.TargetUpdated="DatosEvento_SourceUpdated">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Style="{DynamicResource WrappanelOrganizador}">
                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Nombre Organizador" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Organizer.DisplayName}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Correo Organizador" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Organizer.Email}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Nombre Evento" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Summary}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Estado" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Status}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Fecha Inicio dd/mm/yyyy" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Start.DateTime}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Fecha actualización" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=Updated, ConverterCulture={x:Static SystemGlobalization:CultureInfo.DefaultThreadCurrentCulture}}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Fecha Fin dd/mm/yyyy" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=End.DateTime}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource StackPanelBloqueDatos}">
                                <Border Style="{DynamicResource BordesCategoria}">
                                    <TextBlock Text="Enlace" Style="{DynamicResource ValorCategoria}"/>
                                </Border>
                                <TextBlock Text="{Binding Path=HtmlLink}" Foreground="Blue" TextDecorations="Underline" MouseDown="TextBlockHiperLink_MouseDown" Cursor="Hand"/>
                            </StackPanel>
                        </WrapPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <WrapPanel Orientation="Horizontal" Margin="5 0 0 0">
                <Button x:Name="BotonEditarEvento" 
                        Click="BotonEditarEvento_Click">
                    <TextBlock Text="Editar"
                               x:Name="BotonEditar"
                               Style="{DynamicResource TextoBotonera}"/>
                </Button>
                <Button>
                    <TextBlock Text="Eliminar"
                              Style="{DynamicResource TextoBotonera}"/>
                </Button>
            </WrapPanel>
        </StackPanel>

        <Grid Margin="5" Grid.Row="2" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Nombre Organizador" 
                       Grid.Row="0"/>
            <TextBox Grid.Row="0" 
                     Grid.Column="1"
                     Margin="5 0 5 0"
                     x:Name="TextoOrganizador"/>

            <TextBlock Grid.Row="1" 
                       Text="Asunto:"/>
            <TextBox Grid.Row="1" 
                     Grid.Column="1"  
                     TextWrapping="Wrap"
                     MaxWidth="220"
                     Height="Auto"
                     Margin="5 0 5 0"
                     x:Name="TextoAsunto"/>

            <TextBlock Grid.Row="2"
                       Text="Fecha inicio:"/>
            <DatePicker Grid.Column="1" 
                        Grid.Row="2" 
                        Focusable="False"
                        DisplayDate="{x:Static sys:DateTime.Now}"
                        x:Name="FechaInicioDatePicker"
                        SelectedDateChanged="FechaInicioDatePicker_SelectedDateChanged"
                        Margin="5 0 5 0"/>

            <TextBlock Text="Correo Organizador" 
                       Grid.Row="0"
                       Grid.Column="2"
                       Margin="0 0 5 0"/>
            <TextBox Grid.Row="0" 
                     Grid.Column="3"
                     Margin="5 0 5 0"
                     x:Name="TextoEmail"/>

            <TextBlock Grid.Row="1"
                       Grid.Column="2"
                       Text="Estado:"/>
            <ComboBox Grid.Row="1"
                      Grid.Column="3"
                      Margin="5 0 5 0"
                      x:Name="ComboBoxEstado">
                <ComboBoxItem>Por confirmar</ComboBoxItem>
                <ComboBoxItem>Confirmado</ComboBoxItem>
                <ComboBoxItem>Cancelado</ComboBoxItem>
            </ComboBox>

            <TextBlock Grid.Row="2"
                       Grid.Column="2"
                       Text="Fecha Fin:"/>
            <DatePicker Grid.Column="3"
                        Grid.Row="2" 
                        Focusable="False"
                        IsEnabled="False"
                        x:Name="FechaFinDatePicker"
                        Margin="5 0 5 0"/>
            <TextBlock Text="Hora Inicio"
                       Grid.Row="3"/>
            <WrapPanel Orientation="Horizontal"
                       Grid.Row="3"
                       Grid.Column="1"
                       Margin="5 0 5 0">
                <ComboBox x:Name="ComboBoxHoras"/>
                <ComboBox x:Name="ComboBoxMinutos"/>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal"
                       Grid.Row="3"
                       Grid.Column="3"
                       Margin="5 0 5 0"/>

            <WrapPanel Orientation="Horizontal"
                       Grid.Row="4"
                       Grid.ColumnSpan="4"
                       Margin="0 5 0 0">
                <Button IsEnabled="False"
                        x:Name="BotonAgregarEvento"
                        Click="BotonAgregarEvento_Click">
                    <TextBlock Text="Agregar Evento"
                               Margin="5 0 5 0"/>
                </Button>
                <Button IsEnabled="False">
                    <TextBlock Text="Editar Evento"                          
                               Margin="5 0 5 0"/>
                </Button>
                <Button>
                    <TextBlock Text="Limpiar Campos"
                               Margin="5 0 5 0"/>
                </Button>
            </WrapPanel>

        </Grid>
    </Grid>
</Window>
